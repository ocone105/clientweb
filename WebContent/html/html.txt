1. 기본태그
	h1~h6, p, br, b, i, font, img, a 

2. 목록
	- css를 적용할 때 메뉴로 많이 활용되는 태그
	- 목록으로 표시되는 아이템은 <li></li>안에 정의
	1) 순서가 있는 목록
		<ol></ol>안에 목록을 정의
		
	2) 순서가 없는 목록
		<ul></ul>안에 목록을 정의
3. 표작성
4. 양식태그
	1) Client가 Server와 통신하기 위해서 필요한 태그 
	이 양식 태그를 통해 선택하거나 입력하는 값을 서버로 전송하기 위해서는 무조건 <form></form>태그 사이에 선언해야 한다.
		=> 양식태그에서 선택하거나 입력한 값이 <form>과 </form>안에 정의되어 있어야만 서버로 전송된다.
		그렇지 않은 경우 서버로 전송되지 않는다.
		=><form>태그의 속성에는 action, method, enctype..등이 있으며 
		method는 요청방식을 명시하고 
		action에는 요청할 web application의 path를 정의한다.
	2) 양식태그를 정의하는 경우 name속성을 정의해야 Server로 전송되는 값을 구분할 수 있다.
					   -----------
5. HTML5